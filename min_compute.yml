version: '2.0.0'

compute_spec:

  miner:

    cpu:
      min_cores: 2
      min_speed: 2.5
      recommended_cores: 4
      recommended_speed: 3.5
      architecture: "x86_64"

    gpu:
      required: True
      min_vram: 4
      recommended_vram: 8
      cuda_cores: 2000
      min_compute_capability: 6.1
      recommended_compute_capability: 7.5
      recommended_gpu: "NVIDIA GTX 1660 Super"

    memory:
      min_ram: 8
      min_swap: 2
      recommended_swap: 4
      ram_type: "DDR4"

    storage:
      min_space: 50
      recommended_space: 100
      type: "SSD"
      min_iops: 500
      recommended_iops: 1000

    os:
      name: "Ubuntu"
      version: 22.04

  validator:

    cpu:
      min_cores: 4
      min_speed: 2.5
      recommended_cores: 8
      recommended_speed: 3.5
      architecture: "x86_64"

    gpu:
      required: True
      min_vram: 20
      recommended_vram: 24
      min_compute_capability: 7.5
      cuda_cores: 8000
      recommended_compute_capability: 8.6
      recommended_gpu: "NVIDIA RTX A5000 24GB"

    memory:
      min_ram: 32
      recommended_ram: 64
      min_swap: 8
      recommended_swap: 16
      ram_type: "DDR4"

    storage:
      min_space: 150
      recommended_space: 500
      type: "SSD"
      min_iops: 1000
      recommended_iops: 5000

    os:
      name: "Ubuntu"
      version: 22.04

network_spec:
  bandwidth:
    download: 100
    upload: 20